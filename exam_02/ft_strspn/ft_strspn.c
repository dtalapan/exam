size_t  ft_strspn(const char *s, const char *accept)
{
    size_t  count = 0;
    while (*s1)
    {
        if (ft_strchr(s2, *s1))
            count++;
        else
            break;
        s1++;
    }
    return (count);
}
